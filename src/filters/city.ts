
export const getCityByCode = (id: string | number | undefined): string => {
  console.log(id)
  return 'TODO'
  // if (!cdopm) return 'Não cadastrada'
  // const opm = opms.filter((opm) => opm.value === cdopm)
  // if (!opm.length) return 'Não encontrada'
  // return opm[0].label
}
