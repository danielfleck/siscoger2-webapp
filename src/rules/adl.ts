import { Rules } from './'

export const adlRules = {
  toShow: {
    permissions: [/* 'listar-sidicancias', 'ver-adls' */],
    roles: []
  } as Rules,
  toCreate: {
    permissions: [/* 'listar-sidicancias', 'ver-adls' */],
    roles: []
  } as Rules,
  toEdit: {
    permissions: [/* 'listar-sidicancias', 'ver-adls' */],
    roles: []
  } as Rules,
  toDelete: {
    permissions: [/* 'listar-sidicancias', 'ver-adls' */],
    roles: []
  } as Rules
}
