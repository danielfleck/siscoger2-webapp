import { Rules } from './'

export const authRules = {
  toShow: {
    permissions: [/* 'listar-sidicancias', 'ver-auths' */],
    roles: []
  } as Rules,
  toCreate: {
    permissions: [/* 'listar-sidicancias', 'ver-auths' */],
    roles: []
  } as Rules,
  toEdit: {
    permissions: [/* 'listar-sidicancias', 'ver-auths' */],
    roles: []
  } as Rules,
  toDelete: {
    permissions: [/* 'listar-sidicancias', 'ver-auths' */],
    roles: []
  } as Rules
}
