import { Rules } from './'

export const ipmRules = {
  toShow: {
    permissions: [/* 'listar-sidicancias', 'ver-ipms' */],
    roles: []
  } as Rules,
  toCreate: {
    permissions: [/* 'listar-sidicancias', 'ver-ipms' */],
    roles: []
  } as Rules,
  toEdit: {
    permissions: [/* 'listar-sidicancias', 'ver-ipms' */],
    roles: []
  } as Rules,
  toDelete: {
    permissions: [/* 'listar-sidicancias', 'ver-ipms' */],
    roles: []
  } as Rules
}
