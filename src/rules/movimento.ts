import { Rules } from './'

export const movimentoRules = {
  toShow: {
    permissions: [/* 'listar-sidicancias', 'ver-movimentos' */],
    roles: []
  } as Rules,
  toCreate: {
    permissions: [/* 'listar-sidicancias', 'ver-movimentos' */],
    roles: []
  } as Rules,
  toEdit: {
    permissions: [/* 'listar-sidicancias', 'ver-movimentos' */],
    roles: []
  } as Rules,
  toDelete: {
    permissions: [/* 'listar-sidicancias', 'ver-movimentos' */],
    roles: []
  } as Rules
}
