import { Rules } from './'

export const pendenciaRules = {
  toShow: {
    permissions: [/* 'listar-sidicancias', 'ver-pendencias' */],
    roles: []
  } as Rules,
  toCreate: {
    permissions: [/* 'listar-sidicancias', 'ver-pendencias' */],
    roles: []
  } as Rules,
  toEdit: {
    permissions: [/* 'listar-sidicancias', 'ver-pendencias' */],
    roles: []
  } as Rules,
  toDelete: {
    permissions: [/* 'listar-sidicancias', 'ver-pendencias' */],
    roles: []
  } as Rules
}
