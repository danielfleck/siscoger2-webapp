import { Rules } from './'

export const permissionRules = {
  toShow: {
    permissions: [/* 'listar-sidicancias', 'ver-permissions' */],
    roles: []
  } as Rules,
  toCreate: {
    permissions: [/* 'listar-sidicancias', 'ver-permissions' */],
    roles: []
  } as Rules,
  toEdit: {
    permissions: [/* 'listar-sidicancias', 'ver-permissions' */],
    roles: []
  } as Rules,
  toDelete: {
    permissions: [/* 'listar-sidicancias', 'ver-permissions' */],
    roles: []
  } as Rules
}
