import { Rules } from './'

export const uploadRules = {
  toShow: {
    permissions: [/* 'listar-sidicancias', 'ver-uploads' */],
    roles: []
  } as Rules,
  toCreate: {
    permissions: [/* 'listar-sidicancias', 'ver-uploads' */],
    roles: []
  } as Rules,
  toEdit: {
    permissions: [/* 'listar-sidicancias', 'ver-uploads' */],
    roles: []
  } as Rules,
  toDelete: {
    permissions: [/* 'listar-sidicancias', 'ver-uploads' */],
    roles: []
  } as Rules
}
